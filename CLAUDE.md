# CLAUDE.md
All rules live in [`AGENTS.md`](./AGENTS.md). Read it first, follow it fully.
Agents: `.claude/agents/` (Claude Code) or `.agents/agents/` (Antigravity).
Skills: `.claude/skills/` or `.agents/skills/`.
Session memo: `.claude/memo.md` — read it at session start instead of re-reading chat history.
